version: "3.9"

services:

  enterprise-knowledge-assistant:

    build: .

    container_name: enterprise-knowledge-assistant

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app

    restart: unless-stopped